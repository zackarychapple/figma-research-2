{
  "name": "@agent-specialists/workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Agent Specialists workspace",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.5.4",
    "vitest": "^1.0.0"
  }
}
